# Backend runtime image for the SafeMaster Scheduler API (server/index.js).
FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server

EXPOSE 3001
CMD ["node", "server/index.js"]
